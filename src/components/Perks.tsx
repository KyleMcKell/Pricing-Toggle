import React from "react";

interface Props {
	storage: string;
	usersAllowed: number;
	sendGB: number;
}

export const Perks = (props: Props) => {
	return (
		<div>
			{/* put perks here */}
			{/* another line */}
		</div>
	);
};
